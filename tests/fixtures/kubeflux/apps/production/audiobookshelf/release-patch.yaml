apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: audiobookshelf
  namespace: flux-system
spec:
  values:
    resources:
      limits:
        memory: 500Mi
    portal:
      open:
        enabled: false

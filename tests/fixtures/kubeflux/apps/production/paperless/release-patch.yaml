apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: paperless-ngx
  namespace: flux-system
spec:
  chart:
    spec:
      version: "11.29.10"
  values:
    persistence:
      data:
        existingClaim: paperless-ngx-data
